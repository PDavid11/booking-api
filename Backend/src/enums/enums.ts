export enum AppointmentStatus {
    PENDING = "PENDING",
    CONFIRMED = "CONFIRMED",
    REJECTED = "REJECTED",
    CANCELLED = "CANCELLED"
}

export enum Gender {
    MALE = "MALE",
    FEMALE = "FEMALE"
}

export enum EmployeeRole {
    OWNER = "OWNER",
    EMPLOYEE = "EMPLOYEE"
}

 